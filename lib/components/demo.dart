import 'package:flutter/material.dart';

class DemoW extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("This is demo page"),
    );
  }
}
